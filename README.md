# Shared Actions in Interleaved RPA Executions
Technique for discovering routine types with shared actions from interleaved executions in Robotic Process Automation (RPA).
This repository contains the implementation and datasets (UI logs) for discovering routine types with shared actions from interleaved executions in robotic process automation (RPA).

## Contents

- `gt_models/`: Ground truth models
- `ui_logs/`: Input UI logs
- `utils/`: Utility functions
- `out/`: Output results

## Setup Virtual Environment

Create virtual environment:

```bash
python3 -m venv venv

## Code Execution
- ```python Routine_identification.py```
- ```python Routine_Evaluation.py```
